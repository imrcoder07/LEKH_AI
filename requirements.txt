# --- Flask & Core ---
Flask==3.1.3
flask-cors==6.0.2
Flask-Limiter==4.1.1  # Rate limiting
python-dotenv==1.2.2
Werkzeug==3.1.8
gunicorn==25.3.0           # Production WSGI server
prometheus-flask-exporter==0.23.2  # Application metrics

# --- Database ---
supabase==2.28.3

# --- Cryptography ---
cryptography==46.0.6

# --- OCR (Local preprocessing + Gemini Vision API) ---
opencv-python-headless==4.13.0.92  # Image preprocessing only, no GUI (~30MB)
numpy>=1.21.0                  # Compatible with opencv 4.13
Pillow==12.2.0

# --- LLM Field Extraction ---
google-generativeai==0.8.6  # Gemini Flash Vision — structured extraction (~2MB)

# --- LLM Field Extraction ---
google-generativeai==0.8.6  # Gemini Flash Vision — structured extraction (~2MB)

# --- PDF Support ---
pypdfium2>=5.0.0           # Lightweight PDF page extraction (~5MB)
